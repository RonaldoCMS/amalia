[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd /app && npm install"]

[phases.build]
cmds = ["cd /app/apps/backend && npm run build"]

[start]
cmd = "cd /app/apps/backend && npm run start:prod"