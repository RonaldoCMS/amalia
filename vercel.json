{
  "builds": [
    {
      "src": "apps/frontend/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "apps/frontend/$1" }
  ],
  "installCommand": "npm install && cd packages/shared && npx tsc"
}